import { css } from "lit";

export const paginationStyles = css`

.pagination {
  display: flex;
  flex-direction: row;
  align-items: center;
}

.pagination__next {
  margin-inline-start: auto;
}

.pagination__previous {
  margin-inline-end: auto;
}

`;