---
import { GrantCodesTestimonials } from '@grantcodes/ui/components/testimonials/index.js'

interface TestimonialItem {
  quote: string
  name: string
  role?: string
  company?: string
  avatar?: string
}

interface Props {
  title?: string
  items: TestimonialItem[]
  layout?: 'cards' | 'list'
}

const { title, items, layout = 'cards' } = Astro.props
---

<GrantCodesTestimonials
  title={title}
  items={JSON.stringify(items)}
  layout={layout}
></GrantCodesTestimonials>
