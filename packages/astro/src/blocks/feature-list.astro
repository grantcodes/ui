---
import { GrantCodesFeatureList } from '@grantcodes/ui/components/feature-list/index.js'

interface FeatureItem {
  title: string
  description?: string
  icon?: string
  href?: string
}

interface Props {
  title?: string
  subtitle?: string
  items: FeatureItem[]
  columns?: number
}

const { title, subtitle, items, columns = 3 } = Astro.props
---

<GrantCodesFeatureList
  title={title}
  subtitle={subtitle}
  items={JSON.stringify(items)}
  columns={columns}
></GrantCodesFeatureList>
