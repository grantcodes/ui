/**
 * Type definitions shared across the @grantcodes/astro integration.
 *
 * Phase 29 will populate with:
 * - IntegrationOptions interface (consumer-facing config)
 * - Renderer-related types
 * - Vite config helper types
 */

export {};
