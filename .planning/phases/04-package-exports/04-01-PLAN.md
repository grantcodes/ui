---
phase: 04-package-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/src/exports.test.js
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02

must_haves:
  truths:
    - "A consumer can import '@grantcodes/ui/components/button/button.styles.css' and get the CSS file"
    - "All 34 component CSS files (33 components + footer-column) are importable via package exports"
    - "focus-ring shared styles are importable via package exports"
    - "Existing exports (components JS, styles/base.css, styles/themes/*, fonts/*) continue to resolve"
  artifacts:
    - path: "packages/ui/package.json"
      provides: "Explicit CSS subpath exports for all component styles and focus-ring"
      contains: "button.styles.css"
    - path: "packages/ui/src/exports.test.js"
      provides: "Test validating all CSS exports resolve to existing files"
  key_links:
    - from: "packages/ui/package.json exports"
      to: "packages/ui/src/components/*/*.styles.css"
      via: "explicit subpath exports"
      pattern: "\\.styles\\.css"
---

<objective>
Add explicit package.json exports for all component CSS files and shared styles, then verify all exports (new and existing) resolve correctly.

Purpose: Enable external consumers to import individual component CSS for use outside Lit web components — the core value of this entire migration project.
Output: Updated package.json with CSS exports, test file validating resolution.
</objective>

<execution_context>
@/home/grantcodes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/grantcodes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-package-exports/04-RESEARCH.md
@packages/ui/package.json

<interfaces>
Current package.json exports field:
```json
{
  ".": { "types": "./src/main.d.ts", "import": "./src/main.js" },
  "./icons": { "types": "./src/icons.d.ts", "import": "./src/icons.js" },
  "./styles/base.css": { "import": "./src/css/base.css", "require": "./src/css/base.css" },
  "./styles/themes/*": { "import": "./src/css/themes/*", "require": "./src/css/themes/*" },
  "./components/*": { "import": "./src/components/*", "require": "./src/components/*" },
  "./fonts/*": "./src/fonts/*"
}
```

All 34 component CSS files that need exports:
- accordion/accordion.styles.css
- app-bar/app-bar.styles.css
- avatar/avatar.styles.css
- badge/badge.styles.css
- breadcrumb/breadcrumb.styles.css
- button/button.styles.css
- button-group/button-group.styles.css
- card/card.styles.css
- code-preview/code-preview.styles.css
- container/container.styles.css
- cta/cta.styles.css
- dialog/dialog.styles.css
- dropdown/dropdown.styles.css
- dropzone/dropzone.styles.css
- feature-list/feature-list.styles.css
- footer/footer.styles.css
- footer/footer-column.styles.css
- form-field/form-field.styles.css
- gallery/gallery.styles.css
- hero/hero.styles.css
- icon/icon.styles.css
- loading/loading.styles.css
- logo-cloud/logo-cloud.styles.css
- media-text/media-text.styles.css
- newsletter/newsletter.styles.css
- notice/notice.styles.css
- pagination/pagination.styles.css
- pricing/pricing.styles.css
- sidebar/sidebar.styles.css
- stats/stats.styles.css
- tabs/tabs.styles.css
- testimonials/testimonials.styles.css
- toast/toast.styles.css
- tooltip/tooltip.styles.css

Plus shared style:
- lib/styles/focus-ring.styles.css

Test runner command:
```
NODE_ENV=test node --import @lit-labs/ssr-dom-shim/register-css-hook.js --test --test-concurrency=1 src/**/*.test.js
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CSS exports and write export resolution test</name>
  <files>packages/ui/package.json, packages/ui/src/exports.test.js</files>
  <behavior>
    - Test 1: Every component CSS file (34 total) is resolvable via package.json exports subpath
    - Test 2: focus-ring shared styles are resolvable via a `./styles/focus-ring.css` export
    - Test 3: Existing exports still resolve (./components/button/button.js, ./styles/base.css, ./fonts/*)
    - Test 4: Each exported CSS path points to a file that actually exists on disk
  </behavior>
  <action>
Add 34 explicit CSS subpath exports to package.json `exports` field, one per component CSS file. Place them BEFORE the `"./components/*"` wildcard so they take precedence. Use simple string mappings (not conditional import/require objects) since CSS files are environment-agnostic. Pattern:
```json
"./components/button/button.styles.css": "./src/components/button/button.styles.css"
```

Also add a shared styles export:
```json
"./styles/focus-ring.css": "./src/lib/styles/focus-ring.styles.css"
```

IMPORTANT: Keep all existing exports exactly as they are. The `"./components/*"` wildcard must remain for JS imports. The explicit CSS paths must appear before it in the exports object (Node.js resolves first match for specificity).

Then create `packages/ui/src/exports.test.js` that:
1. Reads package.json exports field
2. For each CSS export entry, verifies the target file exists on disk using `fs.existsSync`
3. Verifies all 34 component CSS files have explicit exports
4. Verifies focus-ring has an export
5. Verifies existing non-CSS exports are still present (spot check: `.`, `./icons`, `./styles/base.css`, `./components/*`)

Use Node.js native test runner (`import { describe, it } from 'node:test'` and `import assert from 'node:assert'`).
  </action>
  <verify>
    <automated>cd packages/ui && node --test src/exports.test.js</automated>
  </verify>
  <done>package.json has explicit exports for all 34 CSS files + focus-ring, test validates all exports resolve to existing files, all existing exports unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify no regressions</name>
  <files>packages/ui/package.json</files>
  <action>
Run the full existing test suite to verify the package.json changes don't break any existing functionality:

1. Run `pnpm test:ui` from the monorepo root (or `pnpm test` from packages/ui)
2. If any test fails, investigate whether the failure is caused by the exports changes and fix

The exports changes should be purely additive — adding new entries to the exports object and not modifying existing ones. No test failures are expected.
  </action>
  <verify>
    <automated>cd packages/ui && pnpm test</automated>
  </verify>
  <done>All existing unit tests pass, lint passes, no regressions from package.json exports changes</done>
</task>

</tasks>

<verification>
1. `node --test packages/ui/src/exports.test.js` passes — all CSS exports resolve
2. `cd packages/ui && pnpm test` passes — no regressions
3. `node -e "const pkg = require('./packages/ui/package.json'); const css = Object.keys(pkg.exports).filter(k => k.endsWith('.css')); console.log(css.length + ' CSS exports')"` shows 36 CSS exports (34 component + focus-ring + base.css)
</verification>

<success_criteria>
- All 34 component CSS files have explicit subpath exports in package.json
- focus-ring shared styles exported as ./styles/focus-ring.css
- Existing exports (JS components, themes, base.css, fonts) unchanged and functional
- Export resolution test exists and passes
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-package-exports/04-01-SUMMARY.md`
</output>
