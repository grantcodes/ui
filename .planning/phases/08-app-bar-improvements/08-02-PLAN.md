---
phase: 08-app-bar-improvements
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/ui/src/components/app-bar/app-bar.stories.js
autonomous: false
requirements: [APPBAR-08]

must_haves:
  truths:
    - "Stories demonstrate the app bar without inline styles on slotted content"
    - "A story shows the real-world <ul> nav pattern matching Astro site usage"
    - "Stories render correctly in Storybook"
  artifacts:
    - path: "packages/ui/src/components/app-bar/app-bar.stories.js"
      provides: "Improved stories without inline styles, with ul nav pattern"
      contains: "slot=\"nav\""
  key_links:
    - from: "app-bar.stories.js"
      to: "app-bar.js"
      via: "import"
      pattern: "import.*app-bar\\.js"
---

<objective>
Improve Storybook stories for the app bar to properly demonstrate component usage patterns without inline styles.

Purpose: Current stories use extensive inline styles on slotted content, masking the component's actual styling behavior. Stories should showcase the component as consumers would actually use it, including the `<ul>` nav list pattern used in the Astro site.

Output: Clean, representative Storybook stories.
</objective>

<execution_context>
@/home/grantcodes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/grantcodes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/ui/src/components/app-bar/app-bar.stories.js
@packages/ui/src/components/app-bar/app-bar.css
@packages/ui/src/components/app-bar/app-bar.component.js
@apps/astro/src/components/Header.astro
@packages/ui/AGENTS.md

<interfaces>
<!-- The component's slot API (from app-bar.component.js render method): -->
Slots: "logo", "nav", "actions"
Properties: sticky (Boolean), transparent (Boolean)
Events: menu-toggle (detail: { open: boolean })

<!-- CSS already styles these slotted patterns (from app-bar.css): -->
::slotted(a) — styled with theme tokens for color, padding, border-radius, transition, font
::slotted(a:hover) — hover state with subtle background
::slotted(ul) — display flex, no list-style, no margin/padding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Storybook stories</name>
  <files>packages/ui/src/components/app-bar/app-bar.stories.js</files>
  <action>
Rewrite `app-bar.stories.js` with these stories:

1. **Default** — Basic app bar with logo text, nav links as direct `<a>` tags in a `<div slot="nav">`, and a Sign In button action. No inline styles on any slotted content — let the component's `::slotted()` rules do the work. The logo `<a>` should just have its text content.

2. **WithListNav** — Matches real Astro site pattern: `<ul slot="nav">` with `<li><a>` children. This is the pattern from `Header.astro`. Shows that `::slotted(ul)` display: flex works.

3. **Sticky** — Same as Default but with `sticky` attribute. Wrap in a tall container (`min-height: 200vh`) with some paragraph content so the sticky behavior is testable by scrolling.

4. **Transparent** — App bar with `transparent` attribute over a colored background div. The background div can have inline styles (it's not a slotted element, it's story framing). Keep the `<a>` tags style-free.

5. **WithMultipleActions** — Multiple buttons in the actions slot. Use `<div slot="actions">` with multiple `<grantcodes-button>` elements. No inline styles.

Remove emoji usage from button labels in the multiple actions story — use text labels like "Notifications", "Settings", "Profile" instead.

Import pattern should match existing Storybook conventions:
```javascript
import { html } from "lit";
import "./app-bar.js";
import "../button/button.js";
```
  </action>
  <verify>
    <automated>node -e "import('./packages/ui/src/components/app-bar/app-bar.stories.js').then(() => console.log('OK')).catch(e => console.error(e.message))" 2>&1 || echo "Module check done"</automated>
  </verify>
  <done>
    - 5 story exports: Default, WithListNav, Sticky, Transparent, WithMultipleActions
    - No inline styles on slotted content (except background wrapper in Transparent story)
    - WithListNav uses `<ul slot="nav">` with `<li><a>` children
    - No emoji in button labels
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of app bar in Storybook</name>
  <action>
Start Storybook and verify the app bar stories render correctly with all CSS fixes applied.
  </action>
  <what-built>Improved app bar styles and stories — all CSS fixes applied, responsive behavior improved, Storybook stories rewritten without inline styles</what-built>
  <how-to-verify>
    1. Run `pnpm dev:ui` to start Storybook
    2. Navigate to Components > App Bar
    3. Check Default story: logo, nav links, and action button render with proper spacing and theme styles
    4. Check WithListNav story: `<ul>` nav items display in a row on desktop
    5. Resize browser narrow to verify:
       - Nav collapses and hamburger button appears
       - Hamburger button is comfortably tappable (44px)
       - Click hamburger: nav opens vertically, `<ul>` items stack
       - Click again: hamburger animates to X and back
    6. Check Sticky story: scroll down, app bar sticks to top
    7. Check Transparent story: app bar has no background, blends with colored section behind it
    8. Check WithMultipleActions: buttons in actions slot display correctly
  </how-to-verify>
  <verify>Human visual verification in Storybook</verify>
  <done>All stories render correctly, responsive behavior works, CSS fixes visible</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All stories render without console errors in Storybook
- No inline `style=""` attributes on slotted elements (except story framing wrappers)
- Visual inspection confirms responsive behavior at narrow/wide viewports
</verification>

<success_criteria>
Stories accurately demonstrate the app bar as consumers would use it. Component looks correct at all viewport sizes in Storybook. Human has verified visual quality.
</success_criteria>

<output>
After completion, create `.planning/phases/08-app-bar-improvements/08-02-SUMMARY.md`
</output>
