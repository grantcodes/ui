---
phase: 02-pilot-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/lib/styles/focus-ring.styles.css
  - packages/ui/src/components/badge/badge.styles.css
  - packages/ui/src/components/badge/badge.component.js
  - packages/ui/src/components/button/button.styles.css
  - packages/ui/src/components/button/button.component.js
autonomous: false
requirements: [SHARED-01, SHARED-02]

must_haves:
  truths:
    - "focus-ring.styles.css exists as a plain CSS file with identical selectors and properties to the JS version"
    - "Badge component renders in Storybook with styles applied using CSS import attributes"
    - "Button component renders in Storybook with both focus-ring and button styles applied using CSS import attributes"
    - "All existing button and badge tests pass without changes to test assertions"
  artifacts:
    - path: "packages/ui/src/lib/styles/focus-ring.styles.css"
      provides: "Shared focus-ring styles as plain CSS"
      contains: ".focus-ring"
    - path: "packages/ui/src/components/badge/badge.styles.css"
      provides: "Badge component styles as plain CSS"
      contains: ".badge"
    - path: "packages/ui/src/components/badge/badge.component.js"
      provides: "Badge component importing CSS via import attributes"
      contains: "with { type: \"css\" }"
    - path: "packages/ui/src/components/button/button.styles.css"
      provides: "Button component styles as plain CSS (without focus-ring — that's a separate import)"
      contains: ".button"
    - path: "packages/ui/src/components/button/button.component.js"
      provides: "Button component importing both focus-ring and button CSS via import attributes"
      contains: "with { type: \"css\" }"
  key_links:
    - from: "packages/ui/src/components/button/button.component.js"
      to: "packages/ui/src/lib/styles/focus-ring.styles.css"
      via: "CSS import attribute"
      pattern: "import focusRingStyles from.*focus-ring\\.styles\\.css.*with.*type.*css"
    - from: "packages/ui/src/components/button/button.component.js"
      to: "packages/ui/src/components/button/button.styles.css"
      via: "CSS import attribute"
      pattern: "import buttonStyles from.*button\\.styles\\.css.*with.*type.*css"
    - from: "packages/ui/src/components/badge/badge.component.js"
      to: "packages/ui/src/components/badge/badge.styles.css"
      via: "CSS import attribute"
      pattern: "import badgeStyles from.*badge\\.styles\\.css.*with.*type.*css"
---

<objective>
Migrate shared focus-ring styles, badge component, and button component from Lit `css` template literals to plain CSS files imported via CSS import attributes.

Purpose: Prove the migration pattern end-to-end with real components — badge validates the simple case (no shared style deps), button validates the shared style chain (imports focus-ring).
Output: Three `.styles.css` files replacing their `.styles.js` counterparts, two updated `.component.js` files using `import ... with { type: 'css' }`.
</objective>

<execution_context>
@/home/grantcodes/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/grantcodes/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-tooling/01-01-SUMMARY.md

<interfaces>
<!-- Pattern established by Phase 1 _css-test fixture -->

From packages/ui/src/components/_css-test/css-test.component.js:
```javascript
import { LitElement, html } from "lit";
import styles from "./css-test.styles.css" with { type: "css" };

export class GrantCodesCssTest extends LitElement {
  static styles = [styles];
  // ...
}
```

From packages/ui/src/lib/styles/focus-ring.styles.js (current, to be replaced):
```javascript
import { css } from "lit";
export const focusRingStyles = css`
  .focus-ring, :host(.focus-ring) { ... }
  .focus-ring:focus-visible, :host(.focus-ring):focus-visible { ... }
  @media (prefers-contrast: more) { ... }
`;
```

From packages/ui/src/components/button/button.styles.js (current, to be replaced):
```javascript
import { css } from "lit";
import { focusRingStyles } from "../../lib/styles/focus-ring.styles.js";
export const buttonStyles = css`
  ${focusRingStyles}
  /* ... button-specific CSS ... */
`;
```
Note: Button interpolates focusRingStyles into its own css template. After migration, these become two separate CSSStyleSheet imports in `static styles = [focusRingStyles, buttonStyles]`.

From packages/ui/src/components/button/button.component.js (current):
```javascript
import { buttonStyles } from "./button.styles.js";
export class GrantCodesButton extends LitElement {
  static styles = [buttonStyles];
  // ...
}
```

From packages/ui/src/components/badge/badge.styles.js (current, to be replaced):
```javascript
import { css } from "lit";
export const badgeStyles = css`...`;
```

From packages/ui/src/components/badge/badge.component.js (current):
```javascript
import { badgeStyles } from "./badge.styles.js";
export class GrantCodesBadge extends LitElement {
  static styles = [badgeStyles];
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate focus-ring shared styles and badge component to CSS import attributes</name>
  <files>packages/ui/src/lib/styles/focus-ring.styles.css, packages/ui/src/components/badge/badge.styles.css, packages/ui/src/components/badge/badge.component.js</files>
  <action>
1. Create `packages/ui/src/lib/styles/focus-ring.styles.css` — extract the CSS from `focus-ring.styles.js`'s template literal. Copy the CSS rules verbatim (selectors, properties, media queries). Do NOT include the `import { css } from "lit"` or template literal wrapper — just the raw CSS. Do NOT delete the old `focus-ring.styles.js` file yet (other components still import it — cleanup happens in Phase 3).

2. Create `packages/ui/src/components/badge/badge.styles.css` — extract the CSS from `badge.styles.js`'s template literal. Copy all CSS rules verbatim (box-sizing reset, :host, .badge, all variant classes, .badge__content). Do NOT delete the old `badge.styles.js` file yet (cleanup in Phase 3).

3. Update `packages/ui/src/components/badge/badge.component.js`:
   - Replace `import { badgeStyles } from "./badge.styles.js"` with `import badgeStyles from "./badge.styles.css" with { type: "css" }`
   - Keep `static styles = [badgeStyles]` unchanged (same variable name, now a CSSStyleSheet instead of CSSResult)
   - Do NOT change any other code in the file

4. Run badge tests to verify: `NODE_ENV=test node --import @lit-labs/ssr-dom-shim/register-css-hook.js --test src/components/badge/badge.test.js` (from packages/ui directory). Tests must pass without any assertion changes.
  </action>
  <verify>
    <automated>cd packages/ui && NODE_ENV=test node --import @lit-labs/ssr-dom-shim/register-css-hook.js --test src/components/badge/badge.test.js</automated>
  </verify>
  <done>focus-ring.styles.css exists with identical CSS to the JS version. badge.styles.css exists with identical CSS. badge.component.js imports via CSS import attributes. All badge tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate button component to CSS import attributes with shared focus-ring dependency</name>
  <files>packages/ui/src/components/button/button.styles.css, packages/ui/src/components/button/button.component.js</files>
  <action>
1. Create `packages/ui/src/components/button/button.styles.css` — extract the CSS from `button.styles.js`'s template literal, but EXCLUDE the focus-ring styles (which were interpolated via `${focusRingStyles}`). The focus-ring styles will be a separate import. Include only the button-specific CSS: the box-sizing reset, :host rules, .button rules, hover/focus/active/disabled states. Do NOT delete the old `button.styles.js` file yet (cleanup in Phase 3).

2. Update `packages/ui/src/components/button/button.component.js`:
   - Remove `import { buttonStyles } from "./button.styles.js"`
   - Add `import focusRingStyles from "../../lib/styles/focus-ring.styles.css" with { type: "css" }`
   - Add `import buttonStyles from "./button.styles.css" with { type: "css" }`
   - Change `static styles = [buttonStyles]` to `static styles = [focusRingStyles, buttonStyles]` — focus-ring first so button styles can override if needed
   - Do NOT change any other code in the file (render method, properties, constructor all stay the same)

3. Run button tests to verify: `NODE_ENV=test node --import @lit-labs/ssr-dom-shim/register-css-hook.js --test src/components/button/button.test.js` (from packages/ui directory). Tests must pass without any assertion changes.

4. Run full test suite to confirm nothing else broke: `pnpm test:unit` (from packages/ui directory). All 237+ tests must pass.
  </action>
  <verify>
    <automated>cd packages/ui && pnpm test:unit</automated>
  </verify>
  <done>button.styles.css exists with button-specific CSS (no focus-ring). button.component.js imports both focus-ring and button CSS via import attributes in static styles array. All button tests pass. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify button and badge render correctly in Storybook</name>
  <action>Visual verification of migrated components in Storybook</action>
  <what-built>Migrated focus-ring shared styles, badge component, and button component from Lit css template literals to plain CSS files with CSS import attributes. Focus-ring is imported as a separate stylesheet in button's static styles array.</what-built>
  <how-to-verify>
    1. Run `pnpm dev:ui` from the repo root to start Storybook
    2. Navigate to Components > Button in the Storybook sidebar
    3. Verify the button renders with full styling (background color, border, padding, border-radius, hover states)
    4. Tab to the button and verify focus-ring outline appears (validates shared style chain)
    5. Navigate to Components > Badge in the Storybook sidebar
    6. Verify the badge renders with styling (colored background, text, rounded corners)
    7. Try different badge variants (primary, success, warning, error, info, neutral) using the controls panel
    8. All visual appearance should be identical to before the migration
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- `packages/ui/src/lib/styles/focus-ring.styles.css` exists with focus-ring CSS
- `packages/ui/src/components/badge/badge.styles.css` exists with badge CSS
- `packages/ui/src/components/badge/badge.component.js` uses `import ... with { type: "css" }`
- `packages/ui/src/components/button/button.styles.css` exists with button CSS (no focus-ring)
- `packages/ui/src/components/button/button.component.js` imports both focus-ring.styles.css and button.styles.css via CSS import attributes
- `pnpm test:unit` passes all tests from packages/ui directory
- Button and badge render correctly in Storybook with all styles applied
</verification>

<success_criteria>
- All four ROADMAP success criteria met:
  1. focus-ring.styles.css exists and is imported by pilot components via CSS import attributes
  2. Button renders correctly in Storybook with CSS import attributes
  3. Badge renders correctly in Storybook with CSS import attributes
  4. Existing button and badge tests pass without assertion changes
- The migration pattern is validated for both the simple case (badge) and the shared-style-dependency case (button + focus-ring)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pilot-migration/02-01-SUMMARY.md`
</output>
