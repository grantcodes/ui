import React from 'react'
import { render, within } from '@testing-library/react'

import { {{pascalCase name}} } from './{{pascalCase name}}'
import { {{pascalCase name}}Props } from './{{pascalCase name}}.types'

describe('{{pascalCase name}}', () => {
  const renderComponent = ({ className }: Partial<{{pascalCase name}}Props>) =>
    render(<{{pascalCase name}} />)

  it('should render heading text correctly', () => {
    const headingText = 'Some test heading'

    const { getByTestId } = renderComponent({ heading: headingText })

    const {{camelCase name}} = getByTestId('test-component__heading')

    expect({{camelCase name}}).toHaveTextContent(headingText)
  }) 
})
